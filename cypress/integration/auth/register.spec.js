describe('Register', () => {
    it('should register a new user', () => {
      // Test code for user registration
    });
  
    it('should display error message with existing email', () => {
      // Test code for registration with existing email
    });
  
    it('should display error message with invalid input', () => {
      // Test code for invalid registration input
    });
  });
  